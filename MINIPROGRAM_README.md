# 智慧党建微信小程序

## 项目简介

基于微信小程序开发的智慧党建管理平台，依托大数据、人工智能、云计算等新一代信息技术，构建"线上+线下"融合、"党建+产业"联动的一体化平台。

### 核心特性

- ✅ **数据安全合规**: 严格遵循《网络安全法》《数据安全法》《个人信息保护法》
- 🔐 **全程加密保护**: 数据采集、存储、传输全流程加密
- 📊 **完整审计日志**: 所有敏感操作全程记录可追溯
- 🛡️ **权限精细管控**: 基于角色的访问控制（RBAC）
- 🚀 **智能化管理**: 党务管理智能化、党员教育精准化
- 🏢 **多级组织支持**: 园区-企业-党员多层级架构

## 技术栈

- **前端框架**: 微信小程序原生开发
- **编程语言**: JavaScript
- **安全加密**: XOR + Base64
- **数据存储**: 微信本地存储（加密）
- **API通信**: HTTPS + 加密传输

## 项目结构

```
miniprogram/
├── app.js                          # 小程序入口文件
├── app.json                        # 小程序配置文件
├── sitemap.json                    # 索引配置
│
├── config/                         # 配置目录
│   └── security.config.js          # 安全配置
│
├── utils/                          # 工具库
│   └── security/                   # 安全工具
│       ├── data-encryption.js      # 数据加密
│       ├── audit-logger.js         # 审计日志
│       └── security-manager.js     # 安全管理器
│
├── services/                       # 业务服务层
│   ├── api.service.js              # API服务
│   ├── auth.service.js             # 认证服务
│   ├── opinion.service.js          # 意见稿服务
│   └── member.service.js           # 党员服务
│
├── models/                         # 数据模型
│   ├── user.model.js               # 用户模型
│   └── member.model.js             # 党员模型
│
├── pages/                          # 页面目录
│   ├── index/                      # 首页
│   ├── login/                      # 登录页
│   ├── enterprise/                 # 企业端
│   │   ├── opinions/               # 意见稿管理
│   │   └── announcements/          # 通知公告
│   └── park/                       # 园区端
│       ├── home/                   # 首页
│       ├── news/                   # 党建资讯
│       ├── members/                # 党员管理
│       ├── organization/           # 组织架构
│       ├── personnel/              # 人员管理
│       └── statistics/             # 数据统计
│
└── components/                     # 组件目录
```

## 功能模块

### 企业用户系统

#### 1. 意见稿模块
- 提交意见稿（主题、内容、附件）
- 查看历史意见稿
- 查看处理状态
- 联系方式管理

#### 2. 通知公告
- 查看公告列表
- 公告详情
- 附件下载

### 园区管理系统

#### 1. 公开首页
- 基本情况展示
- 组织架构图
- GIS地图（企业分布）
- 数据分析看板

#### 2. 意见稿管理
- 查看所有意见稿
- 按机构筛选
- 处理状态跟踪

#### 3. 党建资讯
- 发布资讯
- 设置轮播
- 内容管理

#### 4. 通知公告
- 发布公告
- 附件管理
- 发布时间设置

#### 5. 组织机构维护
- 机构信息管理
- 机构层级关系
- 机构代码管理

#### 6. 人员维护
- 党建工作指导员管理
- 企业网格员管理
- 人员信息导入导出

#### 7. 党员维护
- 党员信息管理
- 批量导入导出
- 敏感信息保护

#### 8. 统计分析
- 数据统计分析
- 可视化报表
- 数据导出

## 数据安全实施

### 1. 数据加密

#### 传输加密
- HTTPS协议
- 请求签名验证
- 时间戳防重放

#### 存储加密
```javascript
// 敏感数据加密存储
DataEncryption.secureStorage.set('token', tokenData);

// 解密读取
const token = DataEncryption.secureStorage.get('token');
```

#### 字段加密
```javascript
// 自动加密敏感字段
const encrypted = securityManager.encryptSensitiveData({
  name: '张三',
  idCard: '110101199001011234',
  phone: '13800138000'
});
```

### 2. 数据脱敏

```javascript
// 身份证脱敏: 110101********1234
member.getMaskedIdCard()

// 手机号脱敏: 138****8000
member.getMaskedPhone()
```

### 3. 审计日志

```javascript
// 记录登录
auditLogger.logLogin(userId, userName, true);

// 记录数据访问
auditLogger.logDataAccess('members', 'list', null);

// 记录数据修改
auditLogger.logDataModification('members', 'update', id, changes);

// 记录数据导出
auditLogger.logDataExport('members', 100, 'excel');
```

### 4. 权限控制

```javascript
// 验证权限
if (!securityManager.validatePermission(userRole, 'export_data')) {
  throw new Error('无权导出数据');
}
```

### 5. 输入验证

```javascript
// 验证输入
const validation = securityManager.validateInput(input, {
  required: true,
  minLength: 2,
  maxLength: 50,
  pattern: /^[\u4e00-\u9fa5a-zA-Z]+$/
});

if (!validation.valid) {
  console.error(validation.errors);
}
```

## 快速开始

### 1. 环境准备

- 安装[微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
- 注册微信小程序账号
- 获取AppID

### 2. 项目配置

1. 克隆项目
```bash
git clone [repository-url]
cd project
```

2. 配置AppID
在 `miniprogram/app.json` 或微信开发者工具中配置您的AppID

3. 配置服务器域名
在微信小程序后台配置合法域名：
- request合法域名
- uploadFile合法域名
- downloadFile合法域名

### 3. 修改配置

编辑 `miniprogram/services/api.service.js`：
```javascript
const BASE_URL = 'https://your-api-domain.com';
```

编辑 `miniprogram/config/security.config.js` 根据需要调整安全配置。

### 4. 运行项目

1. 使用微信开发者工具打开项目
2. 选择 `miniprogram` 目录
3. 点击"编译"按钮
4. 在模拟器或真机上预览

### 5. 测试账号

```
用户名: admin
密码: admin123
角色: 管理员

用户名: park_manager
密码: manager123
角色: 园区管理员

用户名: enterprise
密码: enterprise123
角色: 企业管理员
```

## 开发指南

### 添加新页面

1. 在 `miniprogram/pages/` 创建页面目录
2. 创建必需的文件：
   - `页面名.js` - 逻辑文件
   - `页面名.json` - 配置文件
   - `页面名.wxml` - 结构文件
   - `页面名.wxss` - 样式文件

3. 在 `app.json` 注册页面：
```json
{
  "pages": [
    "pages/新页面/新页面"
  ]
}
```

### 使用安全服务

```javascript
// 导入服务
import { SecurityManager } from '../../utils/security/security-manager';
import { DataEncryption } from '../../utils/security/data-encryption';
import ApiService from '../../services/api.service';

// 使用安全管理器
const app = getApp();
const securityManager = app.globalData.securityManager;

// 验证权限
if (securityManager.validatePermission(role, 'permission_name')) {
  // 执行操作
}

// 验证输入
const result = securityManager.validateInput(input, rules);

// 加密数据
const encrypted = DataEncryption.encrypt(data);

// API请求（自动加密和审计）
const result = await ApiService.post('/endpoint', data);
```

### 添加审计日志

```javascript
const app = getApp();
const auditLogger = app.globalData.auditLogger;

// 记录操作
auditLogger.log({
  action: 'CUSTOM_ACTION',
  module: 'module_name',
  level: 'info',
  details: {
    // 详细信息
  }
});
```

## 安全最佳实践

### 1. 永远不要

- ❌ 在代码中硬编码密钥或密码
- ❌ 在日志中记录敏感信息
- ❌ 信任用户输入
- ❌ 绕过权限检查
- ❌ 在前端存储明文敏感数据

### 2. 始终要

- ✅ 验证所有用户输入
- ✅ 使用HTTPS通信
- ✅ 加密敏感数据
- ✅ 记录审计日志
- ✅ 实施权限控制
- ✅ 定期安全检查

### 3. 敏感操作检查清单

- [ ] 是否需要权限验证？
- [ ] 是否需要记录审计日志？
- [ ] 输入是否已验证？
- [ ] 敏感数据是否已加密？
- [ ] 错误信息是否泄露敏感信息？

## API文档

### 认证相关

```javascript
// 登录
AuthService.login(username, password)

// 微信登录
AuthService.wxLogin(code)

// 退出登录
AuthService.logout()

// 刷新Token
AuthService.refreshToken()

// 获取用户信息
AuthService.getUserInfo()

// 修改密码
AuthService.updatePassword(oldPassword, newPassword)

// 请求数据授权
AuthService.requestDataConsent(dataTypes)
```

### 意见稿相关

```javascript
// 获取意见稿列表
OpinionService.getOpinionList(filters)

// 获取意见稿详情
OpinionService.getOpinionDetail(id)

// 提交意见稿
OpinionService.submitOpinion(opinionData)

// 更新意见稿
OpinionService.updateOpinion(id, opinionData)

// 删除意见稿
OpinionService.deleteOpinion(id)

// 上传附件
OpinionService.uploadAttachment(filePath)
```

### 党员管理相关

```javascript
// 获取党员列表
MemberService.getMemberList(filters)

// 获取党员详情
MemberService.getMemberDetail(id)

// 创建党员
MemberService.createMember(memberData)

// 更新党员
MemberService.updateMember(id, memberData)

// 删除党员
MemberService.deleteMember(id)

// 导入党员
MemberService.importMembers(filePath)

// 导出党员
MemberService.exportMembers(filters)
```

## 测试

### 功能测试

```bash
# 登录功能
1. 使用正确的用户名密码登录
2. 使用错误的用户名密码登录
3. 不同意隐私政策尝试登录
4. 微信快捷登录

# 权限控制
1. 不同角色访问各个页面
2. 尝试访问无权限的功能
3. 查看审计日志记录

# 数据加密
1. 检查本地存储数据是否加密
2. 抓包检查传输数据是否加密
3. 敏感字段是否正确脱敏
```

### 安全测试

```bash
# XSS测试
在各个输入框输入: <script>alert('xss')</script>

# SQL注入测试
在各个输入框输入: ' OR '1'='1

# 权限绕过测试
尝试直接访问需要权限的API

# 频率限制测试
快速连续发起多个请求
```

## 故障排除

### 1. 登录失败

**问题**: 点击登录没有反应

**解决**:
- 检查是否同意隐私政策
- 检查网络连接
- 查看控制台错误信息
- 确认服务器API正常

### 2. 数据加密错误

**问题**: 提示"数据解密失败"

**解决**:
- 清除本地存储
- 重新登录
- 检查加密密钥是否正常生成

### 3. 权限不足

**问题**: 提示"无权访问"

**解决**:
- 确认当前用户角色
- 检查所需权限配置
- 查看审计日志了解详情

### 4. 审计日志丢失

**问题**: 审计日志没有记录

**解决**:
- 检查 `security.config.js` 中 `audit.enabled` 是否为true
- 检查本地存储是否达到上限
- 确认日志同步是否正常

## 部署

### 小程序发布流程

1. **代码审查**
   - 检查代码质量
   - 安全漏洞扫描
   - 性能测试

2. **版本管理**
   - 更新版本号
   - 编写更新日志
   - 打标签

3. **上传代码**
   - 在微信开发者工具中上传
   - 填写版本说明
   - 提交审核

4. **审核发布**
   - 等待微信审核
   - 审核通过后发布
   - 灰度发布（可选）

### 服务器部署

确保服务器端：
- 支持HTTPS
- 配置CORS
- 实施速率限制
- 配置日志系统
- 建立监控告警

## 维护

### 日常维护

- 每日检查审计日志
- 监控系统性能
- 处理用户反馈
- 及时修复bug

### 定期维护

- 每周安全评估
- 每月数据备份
- 每季度安全培训
- 每年合规审计

## 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

## 联系方式

- 项目负责人: [负责人姓名]
- 邮箱: support@example.com
- 电话: 400-xxx-xxxx

## 致谢

感谢所有为本项目做出贡献的开发者。

## 相关资源

- [微信小程序官方文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)
- [网络安全法](http://www.npc.gov.cn/)
- [数据安全法](http://www.npc.gov.cn/)
- [个人信息保护法](http://www.npc.gov.cn/)

---

**版本**: 1.0.0  
**最后更新**: 2024-01-15
